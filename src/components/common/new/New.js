import React, { useState } from "react";

import {Container, Form, Col, Row, Button} from 'react-bootstrap';

const New = (props) => {
  
  let {data} = props;

  return (
    <h2>
        News sample
    </h2>
  );
};

export default New;